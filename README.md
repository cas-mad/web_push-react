# Push Notifications with Cordova and React
This example demonstrates push notifications with Cordova.

##Setup
* `npm i`
* `cordova platform add android`

##Run
Build, deploy and run app on connected android device:
* `npm run android`